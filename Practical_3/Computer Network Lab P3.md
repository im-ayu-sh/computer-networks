### What I fixed so it works on GitHub:
1. **Added ` ```python `** before the first Python code and **` ``` `** at the end of it so it highlights correctly.
2. **Added ` ``` `** at the end of the second Python code so it stops swallowing your images.
3. Changed the messy `<img src="...">` HTML tags into standard Markdown image tags (`![Alt text](./path/to/image.png)`) which GitHub prefers.
4. Removed the stray `**` at the end of `## 2] Character Stuffing`.
